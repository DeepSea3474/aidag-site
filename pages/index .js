import React from 'react';
// Eğer Web3 kodunuz için başka bir import gerekiyorsa buraya ekleyin (örneğin: 'ethers', 'web3modal')

function Home() {
  return (
    // Bütün içeriğiniz TEK BİR ANA ETİKET (div, main, vb.) içinde olmalı
    <div>
      {/* Kendi Web3 bileşenlerinizin ve HTML kodunuzun TAMAMI BURAYA gelecek */}
    </div>
  );
}

export default Home;
